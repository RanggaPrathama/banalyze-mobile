import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // ── Primary - Vibrant Banana Yellow ──
  static const Color primary = Color(0xFFF1C40F);
  static const Color primaryLight = Color(0xFFFFF3B0);
  static const Color primaryDark = Color(0xFFD4AC0D);
  static const Color primarySoft = Color(0xFFFFF9E0);

  // ── Accent ──
  static const Color accent = Color(0xFF2D3436);

  // ── Light mode backgrounds ──
  static const Color background = Color(0xFFFAF9F6);
  static const Color white = Color(0xFFFFFFFF);
  static const Color surface = Color(0xFFFFFFFF);

  // ── Light gradient ──
  static const Color gradientStart = Color(0xFFFFF8DC);
  static const Color gradientMid = Color(0xFFFFFDF5);
  static const Color gradientEnd = Color(0xFFFAF9F6);

  // ── Dark mode backgrounds ──
  static const Color darkBackground = Color(0xFF121212);
  static const Color darkSurface = Color(0xFF1E1E1E);
  static const Color darkCard = Color(0xFF2A2A2A);

  // ── Dark gradient ──
  static const Color darkGradientStart = Color(0xFF1A1A1A);
  static const Color darkGradientMid = Color(0xFF151515);
  static const Color darkGradientEnd = Color(0xFF121212);

  // ── Text ──
  static const Color textPrimary = Color(0xFF1A1A1A);
  static const Color textSecondary = Color(0xFF6B6B6B);
  static const Color textHint = Color(0xFF9E9E9E);
  static const Color textOnDark = Color(0xFFFFFFFF);
  static const Color darkTextPrimary = Color(0xFFF5F5F5);
  static const Color darkTextSecondary = Color(0xFFB0B0B0);
  static const Color darkTextHint = Color(0xFF757575);

  // ── Status ──
  static const Color ripe = Color(0xFF27AE60);
  static const Color unripe = Color(0xFF6AB04C);
  static const Color overripe = Color(0xFFE67E22);
  static const Color error = Color(0xFFE74C3C);

  // ── Misc ──
  static const Color border = Color(0xFFEEEDE8);
  static const Color darkBorder = Color(0xFF3A3A3A);
  static const Color shadow = Color(0x14000000);
  static const Color cardBackground = Color(0xFFFFF9E6);

  // ── Navigation ──
  static const Color navActive = Color(0xFF1A1A1A);
  static const Color navInactive = Color(0xFFBDBDBD);
  static const Color darkNavActive = Color(0xFFF1C40F);
  static const Color darkNavInactive = Color(0xFF636363);
}
